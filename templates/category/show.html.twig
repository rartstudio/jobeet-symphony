{% extends 'base.html.twig' %}

{% block title %}
    Jobs in the {{ category.name }}
{% endblock %}

{% block body %}
    <h4>{{ category.name }}</h4>
    {% include 'job/table.html.twig' with {'jobs' : activeJobs} only %}

    <div>
        {{ knp_pagination_render(activeJobs) }}
    </div>
{% endblock %}
